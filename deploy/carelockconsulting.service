[Unit]
Description=CareLock Consulting RCSA web application
# network-online (not just network.target) so an external PostgreSQL
# (DATABASE_URL) is reachable at first start; on-failure restarts cover any
# residual races.
Wants=network-online.target
After=network-online.target

[Service]
Type=simple
User=carelockconsulting
Group=carelockconsulting
EnvironmentFile=/etc/carelockconsulting/carelockconsulting.env
ExecStart=/usr/local/bin/carelockconsulting
Restart=on-failure
RestartSec=5
# The SQLite database (default backend) lives under
# /var/lib/carelockconsulting; StateDirectory creates it (mode 0700, owned by
# the service user) and keeps it writable even with ProtectSystem=strict. It is
# harmless and unused when DATABASE_URL points at an external PostgreSQL.
StateDirectory=carelockconsulting
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
PrivateTmp=true
# Allow binding low ports (e.g. :80) without running as root.
AmbientCapabilities=CAP_NET_BIND_SERVICE
CapabilityBoundingSet=CAP_NET_BIND_SERVICE

[Install]
WantedBy=multi-user.target
